import { LoadingSpinner, LoadingSpinnerContainer } from "./Spinner-styles";

const Spinner = () => {
  return (
    <>
      <LoadingSpinnerContainer>
        <LoadingSpinner></LoadingSpinner>
      </LoadingSpinnerContainer>
    </>
  );
};

export default Spinner;
